# Multi-FASTA Sequence Analyzer

Professional Streamlit application for multi-FASTA quality control, composition,
visualization, ORF/translation, motif and k-mer analysis, codon usage,
restriction-site screening, primer/oligo screening, comparison, reporting and export.

## Run
```bash
python -m pip install -r requirements.txt
python -m streamlit run app.py
```
